specVersion: "v1"
serviceId: "xronly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "xronly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
